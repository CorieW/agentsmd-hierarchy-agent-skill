interface:
  display_name: 'AGENTS Hierarchy'
  short_description: 'Navigate and validate rules-only layered AGENTS.md files'
  default_prompt: "Use $agentsmd-hierarchy to read this repository's AGENTS.md rules chain, follow directory-level Rules and top-of-file comments, prefer bundled or matching repo CLI helpers for validation and normalization, use sync only to normalize or prune existing AGENTS.md files, add --debug when extra trace detail would help, then validate the result before finishing."
