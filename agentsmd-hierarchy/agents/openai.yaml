interface:
  display_name: 'AGENTS Hierarchy'
  short_description: 'Navigate and prefer bundled CLI helpers to sync, validate, and maintain layered AGENTS.md files'
  default_prompt: "Use $agentsmd-hierarchy to read this repository's AGENTS.md chain, follow the local rules, prefer the bundled or matching repo CLI helpers for sync and validation work before manual AGENTS editing, use sync when AGENTS.md files need to be created or refreshed, add --debug when extra trace detail would help, then validate the result before finishing."
